import styled from 'styled-components';
import { fontColor } from '@/assets/theme';

const HyRegisterWrapper = styled.div`
  .RegisterBtn {
    margin-top: 5px;
    height: 45px;
    background-color: transparent;
    font-weight: 550;
    color: ${fontColor};
  }
`;

export default HyRegisterWrapper;
